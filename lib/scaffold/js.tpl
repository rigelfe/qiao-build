/**
 * @file [desc]
 * @author {{{author}}}({{{email}}})
 */

define(function (require) {

    // 数据容器
    var model = {};

    // dom元素容器
    var view = {};

    /**
     * 初始化页面
     */
    function initView() {

    }

    /**
     * 页面事件绑定
     */
    function bindEvents() {

    }
    
    return {
        enter: function (options) {
            initView();
            bindEvents();
        }
    };
});
